{
  "name": "trailguard",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "app.jsx",
  "scripts": {
    "dev": "bash dev.sh",
    "start": "bash dev.sh",
    "build:js": "esbuild app.jsx --bundle --outfile=dist/app.js --format=iife --target=es2017 --jsx=transform --jsx-factory=React.createElement --jsx-fragment=React.Fragment --minify",
    "build": "npm run build:js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "esbuild": "^0.21.5"
  }
}
